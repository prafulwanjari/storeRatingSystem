1)install both node modules in both frontend and backend folder  by writing  command npm install
2)add .env file in the backend folder  
3)in .env file add  
JWT_SECRET==your secret key
PORT=your port number
MONGODB_URI=your local mongoDB database url
